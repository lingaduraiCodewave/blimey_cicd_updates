name: Tauri Windows Updater

on:
  push:
    tags:
      - "v*"

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Print tag (debug)
        run: echo Tag triggered: ${{ github.ref_name }}
